# Quotes

We store bounded excerpts with a location pointer (page/section/line).

- Use `Q-###.md` files.
- Keep excerpts short and compliant.
- Always include `S-###` source id and a location string.
